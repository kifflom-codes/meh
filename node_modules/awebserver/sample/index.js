const server = require('../index.js');

server.addRoute('/hello/{name}', 'GET', (req, res) => {
	const name = req.query.name;
	console.log(`${name} says hello`);
	res.response(200, `hello ${name}\n`);
});

server.serve(8080);
